
# CLI tool, only in development environment.
# https://goframe.org/docs/cli
gfcli:
  build:
    name: "whatsm"
    arch: "amd64"
    system: "linux"
    # system: "windows"
    cgo: true
    packSrc: "manifest/config"
    path: "./bin"
    version:  "v1.0.1"

  docker:
    build: "-a amd64 -s linux -p temp -ew"
    # build: "-a amd64 -s windows -p temp -ew"
    tagPrefixes:
      - my.image.pub/my-app
  